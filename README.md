It just calculates your AURA...........or maybe AURAS

## 📦 Installation & Setup
1. Clone the repository.
2. Run `npm install` to initialize dependencies.
3. Run `npm run dev` to launch the local vault at `localhost:5173`.
